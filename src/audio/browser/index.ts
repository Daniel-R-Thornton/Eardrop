/**
 * src/audio/browser/index.ts
 * Browser-specific audio adapters.
 * (Currently, browser API usage is embedded in player.ts and recorder.ts
 * which are already small and focused. This directory exists for future
 * extraction of AudioContextAdapter, MediaDevicesAdapter, etc.)
 */
export {};
